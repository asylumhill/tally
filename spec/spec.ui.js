// JavaScript source code
var assert = chai.assert;

describe('TallyUI', function () {
    var tally = null;

    beforeEach(function () {
        tally = new $('#testControl').tally({ fontSize: "20px" }).focus();
    });

    afterEach(function () {
    });

    it('UI Testing Placeholder', function () {
    });
});